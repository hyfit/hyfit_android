package com.example.hyfit_android.goal.info

import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle
import com.example.hyfit_android.databinding.ActivityExerciseInfoMapBinding

class ExerciseInfoMapActivity : AppCompatActivity() {
    private lateinit var binding: ActivityExerciseInfoMapBinding
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)

    }
}